<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Details</title>
    <style>
        p{
            font-size: 1.3em;
        }
    </style>
</head>
<body>
    <div class="jumbotron text-center">
        <h1>Cheap Shop Information</h1>
    </div>
    <div class="row">
        <p>Shop ID:  <% if (typeof (item.shopID) !== "undefined") { %><%= item.shopID %><% } %></p><br>
        <p>Shop Name:  <% if (typeof (item.name) !== "undefined") { %><%= item.name %><% } %></p><br>
        <p>Sub District:  <% if (typeof (item.address.Subdistrict) !== "undefined") { %><%= item.address.subdistrict %><% } %></p><br>
        <p>Building:  <% if (typeof (item.address.building) !== "undefined") { %><%= item.address.building %><% } %></p><br>
        <p>Restaurant Contact No.:  <% if (typeof (item.phone) !== "undefined") { %><%= item.phone %><% } %></p><br>
        <p>Cheap Items Category:  <% if (typeof (item.category) !== "undefined") { %><%= item.category %><% } %></p><br>
        <p>Description:  <% if (typeof (item.description) !== "undefined") { %><%= item.description %><% } %></p><br>
    </div>
        <a href="/edit?_id=<%= item._id %>" >Update the information</a><br><br>
        <a href="/delete?_id=<%= item._id %>&owner=<%= item.ownerID %>">Delete the information</a><br><br>
        <a href="/home">Home</a>
</body>
</html>

